import * as React from "react";
import GridDemo from "./GridDemo";
import Grid from "../Grid";

export default {
  title: "Components/Sticky Grid",
  component: Grid,
};

const Story = () => <GridDemo />;
export const Demo = Story.bind({});
